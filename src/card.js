import React from 'react';
import api_key from './apiNum';

const base_API = 'https://api.priceguide.cards/v1/embedded';

function sportsCard(props) {
    let img = `${base_API}/{props.term}`
    return (
        <div></div>
    )
}

export default sportsCard;